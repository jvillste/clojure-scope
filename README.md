# clojure-scope

## Running Tests

Run all tests with:

```sh
clojure -M:test
```

This executes the test runner (`clojure-scope.test-runner`) which runs all test suites and exits with code 1 if any test fails.
